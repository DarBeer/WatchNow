import { SroallableDirective } from './sroallable.directive';

describe('SroallableDirective', () => {
  it('should create an instance', () => {
    const directive = new SroallableDirective();
    expect(directive).toBeTruthy();
  });
});
